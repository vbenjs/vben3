import { withInstall } from '../index'

import Breadcrumb from './src/Breadcrumb.vue'
import BreadcrumbItem from './src/BreadcrumbItem.vue'

export const VbenBreadcrumb = withInstall(Breadcrumb)
export const VbenBreadcrumbItem = withInstall(BreadcrumbItem)
